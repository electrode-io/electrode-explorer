/* @flow */
import React from "react";

import Flag from "./flag-base";

const Reduced = (props) => {
  return (
    <Flag {...props} outline type="reduced" text="Reduced Price"/>
  );
};

export default Reduced;
