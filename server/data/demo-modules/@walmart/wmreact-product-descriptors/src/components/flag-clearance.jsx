/* @flow */
import React from "react";

import Flag from "./flag-base";

const Clearance = (props) => {
  return (
    <Flag {...props} outline type="clearance" text="Clearance"/>
  );
};

export default Clearance;
