/**
 * Returns an internationalized version of a given string. Stubbed until the i18n/l10n
 * implementation is in place
 *
 * @param {String} str a string to internationalize
 * @returns {String} an internationalized string
 */
export const i18n = (str) => str;
