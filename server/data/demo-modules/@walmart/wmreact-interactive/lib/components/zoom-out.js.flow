/* @flow */
import React from "react";

import Zoom from "./zoom";

/**
Zoom out component
@examples
```jsx
<Zoom.Out />
```
@return {ReactElement} Element tree
@component Zoom.Out
@import {Zoom}
@references Zoom
*/
const ZoomOut = () => (
  <Zoom zoomOut={true} zoomIn={false}>Rollback</Zoom>
);

Zoom.Out = ZoomOut;

export default ZoomOut;
