.cross-links {
  padding: 0 10px 20px 10px;
  font-size: 14px;

  .cross-links-title {
    padding: 12px 0;
  }

  .menu-item {
    float: left;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    padding: 12px 0;
    color: $color-relatedblock-text;
  }
}
