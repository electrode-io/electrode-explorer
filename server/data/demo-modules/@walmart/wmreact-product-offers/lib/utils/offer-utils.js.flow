import { IN_STOCK } from "../enums/availability-status";

const _isInStock = (availabilityStatus) => {
  return availabilityStatus === IN_STOCK;
};

const OfferUtils = {
  isInStock: _isInStock
};

export default OfferUtils;
