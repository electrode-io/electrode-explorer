import { PropTypes } from "react";
import StoreProp from "./stores-prop";

const StoresProp = {
  /**
  List of preferred stores
  */
  preferredStores: PropTypes.arrayOf(PropTypes.shape(StoreProp)),

  /**
  List of near by stores
  */
  nearbyStores: PropTypes.arrayOf(PropTypes.shape(StoreProp)),

  /**
  User location
  */
  location: PropTypes.string.isRequired,
  /**
  Automation id for testing
  */
  dataAutomationId: PropTypes.string,
  /**
  Additional classes
  */
  className: PropTypes.string
};

export default StoresProp;
