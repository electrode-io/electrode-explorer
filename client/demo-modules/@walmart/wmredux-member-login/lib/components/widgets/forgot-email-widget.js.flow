import React from "react";
import {connect} from "react-redux";
import ForgotEmail from "../connected/forgot-email";

const ForgotEmailWidget = (props) => (<ForgotEmail {...props} />);

export default connect(({forgotEmailWidget = {}}) => forgotEmailWidget)(ForgotEmailWidget);
