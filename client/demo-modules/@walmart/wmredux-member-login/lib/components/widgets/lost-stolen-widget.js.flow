import React from "react";
import {connect} from "react-redux";
import LostStolen from "../connected/lost-stolen";

const LostStolenWidget = (props) => (<LostStolen {...props} />);

export default connect(({lostStolenWidget = {}}) => lostStolenWidget)(LostStolenWidget);
