import React from "react";
import {connect} from "react-redux";
import ResetPassword from "../connected/reset-password";

const ResetPasswordWidget = (props) => (<ResetPassword {...props} />);

export default connect(({resetPasswordWidget = {}}) => resetPasswordWidget)(ResetPasswordWidget);
