/* @flow */
import React from "react";

import Flag from "./flag-base";

/**
Rollback flag.
@examples
```jsx
<div style={{padding: 20}}>
  <Flag.Rollback/>
</div>
```
@component Flag.Rollback
@import {Flag}
@playground
Flag.Rollback
```
<div style={{padding: 20}}>
  <Flag.Rollback/>
</div>
```
*/

const Rollback = (props) => {
  return (<Flag {...props} outline type="rollback" text="Rollback"/>);
};

export default Rollback;
