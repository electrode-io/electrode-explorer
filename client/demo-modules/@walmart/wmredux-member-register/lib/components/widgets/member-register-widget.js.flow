import React from "react";
import { connect } from "react-redux";
import MemberRegister from "../connected/member-register";

const MemberRegisterWidget = (props) => (<MemberRegister {...props} />);

export default connect(({memberRegisterWidget = {}}) => memberRegisterWidget)(MemberRegisterWidget);
