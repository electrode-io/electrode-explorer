import React from "react";
import { connect } from "react-redux";
import SignUp from "../connected/sign-up";

const SignUpWidget = (props) => (<SignUp {...props} />);

export default connect(({signUpWidget = {}}) => signUpWidget)(SignUpWidget);
